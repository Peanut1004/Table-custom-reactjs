export const initialState = {
  products: [
    {
      id: 1,
      name: "Iphone",
      price: 20,
    },
    {
      id: 2,
      name: "Samsung",
      price: 20,
    },
  ],
};

export const reducer = (state, action) => {
  switch (
    action.type
    // case ADD_TO_CART
    //   return {
    //     ...state,
    //   };
    // case REMOVE_ITEM
    // default:
  ) {
  }
};
